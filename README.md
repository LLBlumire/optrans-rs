# OpTrans

Generic Operational Transforms, for Rust

See the documentation for `Commit` and `CommitData` for improved validation and stability


## Todo

 - [x] Create `TransformableOperation` and `Operand`
 - [x] Create `Commit`, `CommitData` and implement them as `TransformableOperation` and `Operand`
 - [ ] Implement a data server